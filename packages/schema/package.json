{
  "name": "@open-smp/schema",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "pnpm -w exec vitest run --project unit packages/schema/",
    "test:integration": "pnpm -w exec vitest run --project integration packages/schema/"
  },
  "dependencies": {
    "@open-smp/api-types": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^11.7.1",
    "@types/pg": "^8.15.5",
    "testcontainers": "^11.7.1",
    "vitest": "^4.1.10"
  }
}

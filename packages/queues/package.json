{
  "name": "@open-smp/queues",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "pnpm -w exec vitest run --project unit packages/queues/"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}

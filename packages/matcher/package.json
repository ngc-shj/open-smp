{
  "name": "@open-smp/matcher",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "pnpm -w exec vitest run --project unit packages/matcher/"
  },
  "dependencies": {
    "@open-smp/api-types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "vitest": "^4.1.10"
  }
}
